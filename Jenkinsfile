pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "starting checkout"
            }
        }
        stage('Build') {
            steps {
                echo "building build"
            }
        }
    }
}